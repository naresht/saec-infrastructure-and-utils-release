This is an empty JAR to allow us to use jcl-over-slf4j
instead of the commons-logging implementation.

We could put maven exclusions everywhere, but that
just makes life difficult for the team, so this allows
us to just get on with it.